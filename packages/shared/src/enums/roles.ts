export const WorkspaceRole = {
    ADMIN : "admin",
    MEMBER : "member"
} as const

export const ChannelRole = {
    ADMIN : "admin",
    MEMBER : "member"
} as const

export const ChannelType = {
    OPEN: "open",
    CLOSED: "closed",
    DM: "dm",
} as const