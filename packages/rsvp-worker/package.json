{
  "name": "@ripple/rsvp-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/worker.ts",
  "scripts": {
    "dev": "wrangler dev --port 1998 --inspector-port 9230",
    "deploy": "wrangler deploy",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ripple/shared": "workspace:^",
    "ical.js": "2.2.1",
    "postal-mime": "3.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260828.1",
    "vitest": "4.1.5",
    "wrangler": "4.127.0"
  }
}
